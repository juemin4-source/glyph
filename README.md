# Glyph / 织梦机

本地文件优先的 AI 小说创作工作台。

当前候选版本为 **v0.4.2 Gate B**：作者可以在真实 Markdown 项目中写作，并让只读 AI 搜索、读取当前文件、选区与项目资料，带可检查来源回答问题。Gate B 不具备任何作品写入能力。

## 验收

```bash
npm run accept:gate-a
npm run accept:gate-b
```

完整构建与 Rust 检查见 `CLAUDE.md`。
